import * as z from "zod";

export const {{camelCase name}}DataSchema = z.object({
  name: z.string(),
  url: z.string(),
});
export type {{pascalCase name}}Data = z.infer<typeof {{camelCase name}}DataSchema>;

export const {{camelCase name}}ResponseSchema = z.object({
  count: z.number(),
  next: z.string(),
  previous: z.null(),
  results: z.array({{camelCase name}}DataSchema),
});
export type {{pascalCase name}}Response = z.infer<typeof {{camelCase name}}ResponseSchema>;